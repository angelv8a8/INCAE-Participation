{% extends 'dashboard.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ role.code }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ role.name }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ role.description }}</td>
            </tr>
            <tr>
                <th>Usuarios</th>
                <td>
                
                {% for user in role.users %}
                {{user.fullName}} ({{user.email}}),
                {% endfor %}
                
                </td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('role_index') }}">Volver</a>

    <a class="btn btn-warning" href="{{ path('role_edit', {'id': role.id}) }}">Modfiicar</a>

    {{ include('role/_delete_form.html.twig') }}
{% endblock %}
